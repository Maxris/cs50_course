#include <stdio.h> //libraries is just come code alrady written by someone before, this one include forexample the printf function

int main(void) //main function,void because it doesn't return any value
{
    printf("hello, world!\n"); //instructions are between curly braces; \n moves down the cursor 
    return 0; //if we see any value != 0, it means there is a mistake in the code
}


//manual.cs50.io for manual